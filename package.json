{
  "name": "teamai-desktop",
  "version": "0.0.0",
  "private": true,
  "description": "TeamAI Desktop — 桌面客户端 for Tencent/teamai-cli",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "dependencies": {
    "teamai-cli": "github:littlejcai/teamai-cli#0ce5af152746ce7183c4645a96b8e8ce18d1f398"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
