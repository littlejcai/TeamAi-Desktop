# TeamAI Desktop 项目启动文档

| | |
|---|---|
| 项目名称 | TeamAI Desktop（基于 Tencent/teamai-cli 的桌面客户端） |
| 文档版本 | v1.0 |
| 创建日期 | 2026-09-17 |
| 文档状态 | 已评审待确认 |
| 上游项目 | https://github.com/Tencent/teamai-cli（MIT License） |
| 估算基准 | 2 名全职开发 + 1 名兼职设计（工期按此基准，人数变化需按比例调整） |

---

## 1. 项目概述

### 1.1 一句话定位

为 teamai-cli 提供一个桌面客户端，把"团队 AI 资源管理"从终端命令交互升级为可视化操作：资源可浏览、同步可确认、新人可上手、洞察可阅读。

### 1.2 背景与机会

teamai-cli（4.7k stars）定位为团队级 AI 工具资源管理器，管理 skills/rules/MCP/知识，支持 12+ 种 AI Agent（Claude Code、Codex、Cursor、CodeBuddy 等），以共享 Git 仓库为唯一服务端。目前只有 CLI 形态，存在明确的 GUI 缺口：

- **可发现性差**：`teamai list` 纯文本输出，浏览数百个 skill/rule 体验差；
- **上手门槛高**：`init` 的角色选择、`push` 的命名空间选择均为终端交互菜单，非终端用户（使用 Cursor 的产品/运营）无法自助完成；
- **状态不可见**：本地与远端的差异、MR 状态、hooks 健康度、recall 索引健康度全靠命令逐个查询；
- **上游已自证 GUI 需求**：`teamai dashboard` 内置了本地 Web 服务（端口 3721）渲染会话面板与趋势，说明可视化是真实需求，本项目是把这个原型做成正式产品。

### 1.3 项目目标

1. 团队成员（含非终端用户）通过 GUI 独立完成 `init → 日常 pull → push 贡献` 全流程；
2. MVP 阶段覆盖 CLI 高频命令 80% 以上的使用场景；
3. 核心业务逻辑零重写——复用 vendored 的 teamai-cli 源码，只新增 JSON 输出层；
4. MVP 结束时完成 10 人团队灰度，崩溃率 < 1%/会话，主干任务成功率 ≥ 90%。

### 1.4 非目标（明确不做什么）

- **不做自建后端/数据库**：延续上游"Git 仓库即服务端"的架构，不引入账号体系；
- **不做 AI Agent 本身**：不重复造 Claude Code/Cursor 的功能，只做资源管理层；
- **Web 门户不在 MVP 范围**：仅在触发条件满足时启动（见 M3）;
- **不深度修改上游内核**：对 vendored core 的改动限制在输出层（`--json`），保持向上游合并的能力。

---

## 2. 决策依据：第一性原理分析（摘要）

### 2.1 本质

剥掉所有功能，teamai-cli 只做三件事：

1. **本地文件编排**：把 skills/rules/MCP/hooks 写入本机各 AI 工具目录（`.claude/`、`.cursor/`、`~/.teamai/` 等）；
2. **Git 同步**：以共享 Git 仓库为服务端，push/pull/MR 完成团队协作；
3. **本地索引检索**：BM25 + 图增强的知识召回，索引与会话数据全在本机。

### 2.2 数据的二分法（决定产品形态的核心依据）

| 数据 | 存储位置 | 浏览器可访问 |
|---|---|---|
| 资源文件、hooks、MCP 配置、本地索引 | 开发者本机 | ❌ 沙箱无法触达 |
| 团队知识、会话统计、投票、learnings | Git 仓库的资源分支 + `teamai-reports` 孤儿分支 | ✅ 纯读 Git 即可渲染 |

**推论**：

1. 核心动作（安装资源、注入 hooks、pull/push）必须在本机发生 → **桌面客户端才能覆盖完整能力**，纯 Web 只能做只读门户；
2. GUI 的价值不在"能用"（hooks 已让日常流程自动化），而在**可见与可信**：资源可浏览、状态可确认、新人有向导；
3. `teamai dashboard` 已内置本地 Web 服务 → 最经济路径是"桌面壳 + 前端"，而非从零造服务端。

**结论：桌面客户端为主（Electron 优先），Web 只做后续可选的团队只读门户。**

---

## 3. 用户与核心场景

### 3.1 用户画像

| 角色 | 特征 | GUI 关键诉求 |
|---|---|---|
| 团队管理员 | 熟终端，配置 roles/projects/tags，评审贡献 | MR 可视化评审、digest 周报、成员状态一览 |
| 开发者成员 | 熟终端但不想记命令，日常被 hooks 自动化覆盖 | 资源库搜索、一键启停、同步状态确认 |
| 非终端成员（PM/运营/设计） | 用 Cursor 但不开终端 | **初始化向导**、资源浏览/订阅、知识搜索 |

### 3.2 核心场景（MVP 必须打通）

1. **新人入职**：打开客户端 → 粘贴团队仓库地址 → 选角色 → 勾选 AI 工具 → 完成初始化，全程不碰终端；
2. **日常同步**：客户端常驻托盘显示同步状态 → 会话启动时 hooks 自动 pull → 状态图标可一眼确认；
3. **贡献回团队**：本地新写了 skill → 在客户端看到变更 diff → 选命名空间 → 一键 push 生成 MR → 回跳 Git 托管平台评审。

---

## 4. 总体架构

### 4.1 分层架构

```
┌─────────────────────────────────────────────────┐
│  Renderer（React + TS + Vite）                   │
│  资源库 │ 同步中心 │ 初始化向导 │ 知识面板 │ 设置   │
└───────────────────┬─────────────────────────────┘
                    │ IPC（类型契约：packages/ipc-contract）
┌───────────────────┴─────────────────────────────┐
│  Electron Main（Node）                           │
│  命令编排 │ 状态缓存 │ 文件监听 │ 托盘 │ 自动更新  │
└───────────────────┬─────────────────────────────┘
                    │ 直接 import
┌───────────────────┴─────────────────────────────┐
│  packages/core（vendored teamai-cli + JSON 输出层）│
│  pull/push/init │ recall │ dashboard-collector   │
└───────────────────┬─────────────────────────────┘
                    │ 读写
┌───────────────────┴─────────────────────────────┐
│  本机：各 AI 工具目录 / ~/.teamai/                │
│  远端：团队 Git 仓库（资源分支 + teamai-reports）  │
└─────────────────────────────────────────────────┘
```

### 4.2 关键集成决策

| 决策点 | 结论 | 理由 |
|---|---|---|
| 桌面壳 | **Electron**（备选：Tauri 2 + Node sidecar） | CLI 是 TS/Node，Electron 主进程自带 Node，可直接 `import` core，集成成本最低；Tauri 仅在企业对包体/签名有硬约束时切换 |
| 逻辑复用方式 | **vendor 源码**进 monorepo，不 shell 调用 CLI | CLI 输出是给人看的纯文本且无 `--json`，解析文本不可靠；MIT 协议允许 vendor |
| 数据接口 | **新增 JSON 输出层**，不改内核 | 输出层是纯增量，向上游提 PR 后可长期减少分叉 |
| dashboard | **复用 `dashboard-collector`** 数据聚合层 | 会话/趋势/成本估算的采集逻辑已存在，前端重写即可 |
| 锁定上游 | 锁定 commit + 每双周同步一次 | 上游迭代快（776+ commits），防止无序漂移 |

### 4.3 仓库结构（monorepo）

```
TeamAi-Desktop/
├── apps/
│   └── desktop/            # Electron 应用
│       ├── main/           # 主进程（命令编排、托盘、文件监听）
│       ├── preload/        # contextBridge
│       └── renderer/       # React 前端
├── packages/
│   ├── core/               # vendored teamai-cli（只加 --json 输出层）
│   ├── ipc-contract/       # 主/渲染进程共享的 IPC 类型与校验
│   └── ui/                 # 共享 UI 组件（为 M3 Web 门户预留复用）
├── docs/                   # 本文档及后续设计文档
├── scripts/                # 上游同步、打包等脚本
└── package.json            # pnpm workspace
```

### 4.4 技术栈清单

| 层 | 选型 |
|---|---|
| 桌面壳 | Electron + electron-builder |
| 前端 | React 18 + TypeScript + Vite + Tailwind CSS + shadcn/ui |
| 状态管理 | Zustand（轻量，适合 IPC 驱动的状态同步） |
| monorepo | pnpm workspace + Turborepo |
| 测试 | Vitest（core 输出层快照测试）+ Playwright（E2E 主干路径） |
| CI/CD | GitHub Actions：lint / test / build / 打包产物 |
| 版本 | changesets + conventional commits |

---

## 5. 里程碑总览

> 时间为预估（基准：2 全职开发），含 2026 国庆假期缓冲（10-01 ~ 10-08 不排任务）。

| 里程碑 | 周期 | 时间（2026） | 发布物 | 核心退出标准 |
|---|---|---|---|---|
| **M0 立项验证** | 2 周 | 09-17 ~ 09-30 | 技术验证报告 + 可点击原型 + 上游 `--json` PR | 三个 spike 全部通过（见 6.1） |
| **M1 MVP 灰度** | 6 周（3 个双周 Sprint） | 10-09 ~ 11-20 | v0.5 桌面客户端，10 人灰度 | 灰度用户 GUI 跑通全流程，无 P0 崩溃 |
| **M2 知识与洞察** | 4 周 | 11-23 ~ 12-18 | v1.0 正式版 | 知识面板 + digest 上线，全团队推广 |
| **M3 Web 团队门户** | 4~6 周 | 待触发 | Web 只读门户 | 满足触发条件后才立项（见 6.4） |

---

## 6. 里程碑任务分解

### 6.1 M0 立项验证（09-17 ~ 09-30，2 周）

目标：用最小成本验证三大技术风险 + 验证用户需求，避免带病开工。

| # | 任务 | 主要工作 | 产出物 | 预估 |
|---|---|---|---|---|
| M0-1 | 建仓与 vendor | 搭建 monorepo 骨架；锁定上游 commit 并 vendor 进 `packages/core`；跑通其原有测试 | 仓库骨架 + core 测试全绿 | 2 人日 |
| M0-2 | JSON 输出层 | 为 `status --all`、`list`、`pull --dry-run`、`members`、`mcp`、`hooks` 增加结构化 JSON 输出；补快照测试 | core 输出层 + 测试 | 4 人日 |
| M0-3 | Spike①：Electron 直接 import core | 验证 WASM tree-sitter 在 Electron 主进程的加载、打包（asar）与签名兼容性 | Spike 报告 + demo | 2 人日 |
| M0-4 | Spike②：dashboard 复用度评估 | 运行 `teamai dashboard`，梳理 `dashboard-collector` 的数据接口与 `dashboard-html` 的页面功能，标定可复用清单 | 复用清单文档 | 1 人日 |
| M0-5 | Spike③：分发通道 | electron-builder 出包，验证 Windows 安装/卸载/自动更新；评估企业内网签名约束（若有则同步评估 Tauri 备选） | 分发方案结论 | 2 人日 |
| M0-6 | 原型与需求验证 | 资源库/同步中心/初始化向导三页高保真原型；找 3~5 名同事（至少 1 名非终端用户）做 15 分钟可用性走查 | 走查结论 + 页面优先级修正 | 3 人日 |
| M0-7 | 上游回馈 | 将 `--json` 输出层整理为 PR 提交至 Tencent/teamai-cli | 上游 PR 链接 | 1 人日 |

**退出标准（全部满足才进入 M1）**：
- [ ] Spike①②③ 均通过或明确备选方案；
- [ ] 原型走查完成，核心页面信息架构获目标用户认可；
- [ ] `--json` 输出覆盖 M1 所需全部命令并有快照测试。

### 6.2 M1 MVP 灰度（10-09 ~ 11-20，6 周）

目标：可日常替代命令行的桌面客户端 Beta，10 人团队灰度。

**Sprint 1（10-09 ~ 10-23）：应用骨架 + 资源库**

| # | 任务 | 产出物 | 预估 |
|---|---|---|---|
| M1-101 | Electron + Vite + React 工程搭建；IPC 契约包（`packages/ipc-contract`）；安全基线（contextIsolation、sandbox） | 可运行的应用骨架 | 3 人日 |
| M1-102 | 打包流水线：electron-builder + CI 产物 + 自动更新通道 | 每次合码产出可安装包 | 2 人日 |
| M1-103 | 资源库页面 v1：列表/搜索/筛选（按 agent、类型 skill/rule/MCP、来源 repo/local）、详情查看（SKILL.md 渲染） | 资源库可用 | 5 人日 |
| M1-104 | 同步中心 v1：`status --all` 状态一览、pull 按钮 + `--dry-run` diff 视图（本地 vs 远端变更逐条展示） | 同步中心可用 | 4 人日 |
| M1-105 | 设计走查 + Sprint 1 内测 | 修复清单 | 1 人日 |

**Sprint 2（10-26 ~ 11-06）：核心闭环（初始化 + 贡献）**

| # | 任务 | 产出物 | 预估 |
|---|---|---|---|
| M1-201 | 初始化向导：粘贴仓库地址 → 克隆 → 角色选择（roles.yaml 解析）→ AI 工具勾选 → hooks 注入结果确认页 | 新人零终端完成 init | 6 人日 |
| M1-202 | push 流程：本地变更扫描 → diff 确认 → 命名空间选择 → push 生成 MR → 回跳 Git 托管平台 | 贡献闭环 | 5 人日 |
| M1-203 | 系统托盘：常驻同步状态图标、一键 pull、未读 MR 提示 | 托盘功能 | 3 人日 |
| M1-204 | 设计走查 + Sprint 2 内测 | 修复清单 | 1 人日 |

**Sprint 3（11-09 ~ 11-20）：灰度与打磨**

| # | 任务 | 产出物 | 预估 |
|---|---|---|---|
| M1-301 | 设置页：scope 切换（项目/用户级）、代理、语言、日志级别、数据目录展示 | 设置页 | 3 人日 |
| M1-302 | 诊断页：集成 `doctor`，hooks 健康度、各 AI 工具配置检测、一键复制诊断信息 | 诊断页 | 3 人日 |
| M1-303 | 崩溃上报与反馈入口（本地日志 + 手动导出，不建后端） | 反馈通道 | 2 人日 |
| M1-304 | **10 人团队灰度发布**；周度回访；P0/P1 修复 | 灰度报告 | 5 人日 |
| M1-305 | E2E：Playwright 覆盖初始化向导/同步中心/资源库主干路径 | CI E2E 全绿 | 3 人日 |

**M1 退出标准**：
- [ ] ≥ 8/10 灰度用户通过 GUI 独立完成 init → 日常 pull → push 全流程；
- [ ] 主干任务成功率 ≥ 90%，崩溃率 < 1%/会话；
- [ ] 冷启动 < 3s，资源搜索 < 500ms（本地索引）；
- [ ] E2E 主干路径 CI 全绿。

### 6.3 M2 知识与洞察（11-23 ~ 12-18，4 周）

目标：把 GUI 从"资源管理"扩展到"知识资产"，发布 v1.0 正式版并全团队推广。

| # | 任务 | 产出物 | 预估 |
|---|---|---|---|
| M2-401 | recall 搜索 UI：搜索框 + 结果卡片（来源标注 `[project]/[user]`、相关度分数、命中词高亮） | 知识搜索 | 4 人日 |
| M2-402 | learnings 管理列表：查看/点赞/contribute 流程、recall 维护操作（prune/archive/promote） | 学习成果管理 | 4 人日 |
| M2-403 | digest 周报页 + 会话时间线（复用 dashboard-collector 与 `teamai-reports` 分支数据，展示前走既有脱敏管线） | 洞察页 | 5 人日 |
| M2-404 | dashboard 整合：实时成员会话状态、人工干预指标、7 日趋势与成本估算 | 可视化面板 | 4 人日 |
| M2-405 | v1.0 打磨：性能、空态/错误态、快捷键、引导；全团队推广 + 培训材料 | **v1.0 正式版** | 6 人日 |

**M2 退出标准**：
- [ ] 知识搜索/洞察页上线并纳入全团队推广；
- [ ] recall 日均 GUI 调用、digest 周打开率形成基线指标；
- [ ] v1.0 通过完整回归（单测 + E2E + 手工清单）。

### 6.4 M3 Web 团队门户（待触发，预估 4~6 周）

**不排期**。满足任一触发条件时再立项：

1. 需要 SSO 登录与权限隔离（跨团队/跨公司访问）；
2. 管理层需要不安装客户端的聚合视图；
3. 需要向非安装用户（外部协作者）展示团队知识资产。

方案约束：Next.js 只读渲染 Git 仓库（资源分支 + `teamai-reports` 孤儿分支），**不引入数据库**；UI 层复用 `packages/ui`。若最终需要写操作或登录，则升级为独立立项重新评审。

---

## 7. 风险登记册

| # | 风险 | 概率 | 影响 | 应对措施 |
|---|---|---|---|---|
| R1 | 上游迭代快（776+ commits），vendored core 合并冲突越积越大 | 高 | 高 | 只加输出层不改内核；锁定 commit 每双周同步；输出层 PR 回馈上游，被合并后可直接删本地补丁 |
| R2 | CLI 无稳定 JSON 契约，内核内部结构变化破坏输出层 | 中 | 高 | `ipc-contract` 契约测试 + 输出层快照测试；上游同步 CI 门禁 |
| R3 | WASM tree-sitter 在 Electron 打包（asar/签名）下不兼容 | 中 | 高 | M0 Spike① 前置验证；备选方案：主进程 shell 调用编译好的 CLI 单文件（bun compile） |
| R4 | 企业内网对 Electron 包体（~100MB）/签名/自动更新有管控 | 中 | 中 | M0 Spike③ 提前摸清；不满足则切换 Tauri 2 + Node sidecar（~10MB） |
| R5 | 12+ AI 工具的配置格式随版本变化，hooks 注入失效 | 中 | 中 | 诊断页（doctor）内置健康检测；维护工具版本兼容矩阵；异常时降级提示而非静默失败 |
| R6 | 会话统计/reports 数据在 GUI 展示引发隐私顾虑 | 中 | 中 | 展示前强制走上游既有脱敏管线；设置中提供展示开关；M2 上线前做一次合规走查 |
| R7 | 国庆假期 + 关键人员单点导致 M1 排期滑坡 | 中 | 中 | 国庆不排任务已计入缓冲；Sprint 级燃尽跟踪；核心模块双人 familiarity（code review 交叉） |
| R8 | 用户实际不需要 GUI（hooks 已自动化，日常打开频率低） | 低 | 高 | M0-6 原型走查提前证伪；托盘常驻 + 状态可见是留存抓手；若证伪及时收缩为"轻量面板"而非全功能客户端 |

---

## 8. 质量与验收标准

| 维度 | 标准 |
|---|---|
| 单元测试 | core 输出层行覆盖 ≥ 80%；ipc-contract 契约测试全绿 |
| E2E | 初始化向导、同步中心、资源库、push 四条主干路径 Playwright 全绿 |
| 性能 | 冷启动 < 3s；资源/知识搜索 < 500ms；内存峰值 < 500MB |
| 稳定性 | 崩溃率 < 1%/会话（灰度期统计） |
| 安全 | contextIsolation + sandbox 开启；渲染进程无 Node 权限；IPC 入参 schema 校验 |
| 兼容 | Windows 10/11 优先（当前开发环境），macOS 次之；Node ≥ 20、git 为用户前置依赖 |

---

## 9. 协作流程

- **分支模型**：`main` 保护 + `feature/*` 短分支；conventional commits；changesets 管理版本号；
- **上游同步**：每双周一次，`scripts/sync-upstream` 拉取上游新 tag → rebase `packages/core` 输出层补丁 → CI 全绿后合入；冲突超过 200 行时升级评审是否调整策略；
- **评审要求**：`packages/core` 改动必须双人评审；renderer 改动单人评审 + 设计走查；
- **CI 门禁**：lint + 单测 + E2E + 打包冒烟，全绿才可合入 `main`；
- **周会**：Sprint 计划/回顾各 30 分钟；里程碑退出标准逐条打勾后才切换下一阶段。

---

## 10. 待确认事项（开工前需拍板）

1. **灰度团队人选**：M1 的 10 人灰度用哪个团队/项目？需要包含至少 2 名非终端用户；
2. **目标平台优先级**：是否确认 Windows 优先、macOS 随后？（影响 M0 Spike③ 与签名证书申请）
3. **上游 PR 策略**：`--json` 输出层以什么粒度提 PR（单命令一个 PR vs 汇总 PR）？
4. **设计资源**：兼职设计的投入节奏（建议 M0 每周 ≥ 2 天，M1 Sprint 内随版本走查）；
5. **自动更新通道**：企业内网是否允许 GitHub Releases 拉取更新，还是需要内网镜像？

---

## 附录

- 上游使用指南：https://github.com/Tencent/teamai-cli/blob/main/docs/usage-guide.md
- 关键数据位置：项目资源在各工具目录；机器数据在 `~/.teamai/`；会话/投票/统计在 Git 仓库 `teamai-reports` 孤儿分支；请求明细在 `~/.teamai/dashboard/requests.jsonl`（90 天自动清理）
- CLI 无 `--json` 输出（文档确认），这是本项目需要新增输出层的直接原因
- 许可证：上游 MIT，vendor 时保留版权声明与本仓库 LICENSE 说明
